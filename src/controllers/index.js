const scrapEN = require('./scrapEN');
const scrapEN_DEV = require('./scrapEN_DEV');
//const modeDependentDecorator = require('./modeDependentDecorator');

module.exports = {
  scrapEN,
  scrapEN_DEV,
};
